{% extends 'base.html.twig' %}

{% block title %}Peinture{% endblock %}

{% block body %}
    <h1>Peinture</h1>

    <table class="table">
    <div class="card mb-3 text-center">
		<div class="row g-0">
			<div class="col-md-4">
				<img src="{{ peinture.photo }}" class="img-fluid rounded-start" alt="{{ peinture.nom }}">
			</div>
			<div class="col-md-8">
				<div class="card-body">
					<h5 class="card-title">{{ peinture.nom }}</h5>
					<p class="card-text">Description:
						{{ peinture.description }}</p>
					<p class="card-text">{{ peinture.prix|number_format(2, '.',) }}€</p>
				</div>
			</div>
		</div>
	</div>
        
    </table>

    <a href="{{ path('peinture_index') }}">back to list</a>

    <a href="{{ path('peinture_edit', {'id': peinture.id}) }}">edit</a>

    {{ include('peinture/_delete_form.html.twig') }}
{% endblock %}
